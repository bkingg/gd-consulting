---
label: General
hide_body: true
fields:
- type: file
  name: logo
  label: Logo
  config:
    maxSize: 1
- name: background_image_404
  type: file
  config:
    maxSize: 2
  label: Background Image 404
- name: background_image_a_propos
  type: file
  config:
    maxSize: 2
  label: Background Image A Propos
- name: background_image_contact
  type: file
  config:
    maxSize: 2
  label: Background Image Contact
- name: background_image_avantage
  type: file
  config:
    maxSize: 2
  label: Background Image Avantage
pages:
- _data/general.json
